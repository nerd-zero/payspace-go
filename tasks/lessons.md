# Lessons

## Documentation Maintenance

- **Always update `llms.txt`** when making major code changes — new services, new types, renamed fields, new client options, or changed method signatures. The `llms.txt` is the LLM-facing API reference and must stay in sync with the actual code.
